import { getBooking,updateBooking } from '../db.js'; import { cancelFlight,cancelStay } from './duffel.js'; import { refundPayment } from './payment.js';
export async function cancelBooking(id:string,phone:string){const b=await getBooking(id);if(b.user_phone!==phone)throw new Error('Booking not found');if(b.status==='CANCELLED')return `Booking ${id} is already cancelled.`;if(b.status!=='CONFIRMED')throw new Error(`A ${b.status} booking cannot be cancelled`);const hotel=b.hotel_snapshot as {bookingId?:string}|null;if(hotel?.bookingId)await cancelStay(hotel.bookingId);if(b.duffel_order_id)await cancelFlight(b.duffel_order_id);if(b.stripe_session_id)await refundPayment(b.stripe_session_id);await updateBooking(id,{status:'CANCELLED'});return `Booking ${id} is cancelled. Any booked stay and flight were cancelled, and a Stripe test refund was created.`}
